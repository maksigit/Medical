+b.SECTION.offer
  .container-main
    +e.H2.masthead Интересные предложения:
    +e.UL.hits
      each element in ["Все новинки ", "Все хиты продаж", "Лучшая цена"]
        +e.LI.item
          +e.A.link(href="#")= element
    +b.offer-wrap
      .test-grid
        .l-wrap
          for name in hits
            .wrap-grid-item
              .grid-item
                +item(name)